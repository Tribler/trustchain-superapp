package nl.tudelft.ipv8.android.demo.ui.bitcoin

interface BitcoinView {
    /**
     * The controller is an empty view that holds a container. The container is used as main view,
     * each .xml file is displayed in that view.
     */
    val controller: BitcoinViewController
}
