CREATE TABLE tokens(
    id TEXT NOT NULL,
    publicKey TEXT NOT NULL,
    tokenIntId INTEGER NOT NULL,
    PRIMARY KEY (id)
);

getAllTokens:
SELECT id, publicKey, tokenIntId FROM tokens;

addToken:
INSERT INTO tokens(id, publicKey, tokenIntId)
VALUES(?,?, ?);


removeTokenByID:
DELETE FROM tokens WHERE id = ?;

removeToken:
DELETE FROM tokens
    ORDER BY id
    LIMIT 1;

removeAllTokens:
DELETE FROM tokens;

getBalance:
SELECT COUNT(id) FROM tokens;